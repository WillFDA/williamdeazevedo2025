---
interface Props {
    class?: string;
}

const { class: className } = Astro.props;
---

<h2
    class:list={[
        "max-w-[872px] text-center text-3xl font-medium leading-[0.96] text-gray-900 sm:text-4xl md:text-[56px] 2xl:text-[69px]",
        className,
    ]}
>
    <slot />
</h2>
