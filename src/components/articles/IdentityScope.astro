<div class="identity-scope">
  <p class="scope-kicker">Deux périmètres à vérifier dans les devis</p>
  <p class="scope-intro">L’identité donne les repères. Le site les met en usage.</p>
  <p class="scope-hint">Comparez les livrables ; repliez un volet pour vous concentrer sur l’autre.</p>
  <details open>
    <summary><span class="scope-number" aria-hidden="true">01</span><span class="scope-title">Identité visuelle : quels livrables ?</span></summary>
    <div class="scope-content">
      <dl>
        <div><dt>Logo</dt><dd>Création ou évolution, déclinaisons et fichiers exploitables. Persistance prévoit notamment des formats vectoriels et PNG transparents.</dd></div>
        <div><dt>Palette et typographies</dt><dd>Les couleurs et les caractères qui identifient l’entreprise. Faites préciser les références, les licences et les usages prévus, notamment sur le web.</dd></div>
        <div><dt>Charte graphique</dt><dd>Les règles pour utiliser ces éléments de façon cohérente. Demandez quels supports et quelles déclinaisons sont compris dans votre devis.</dd></div>
      </dl>
      <p class="scope-question"><strong>À demander</strong> « Quels fichiers et quelles consignes pourrai-je transmettre à la personne qui réalise mon site ? »</p>
    </div>
  </details>
  <details open>
    <summary><span class="scope-number" aria-hidden="true">02</span><span class="scope-title">Site internet : quel travail à partir de cette base ?</span></summary>
    <div class="scope-content">
      <dl>
        <div><dt>Structure et contenus</dt><dd>Hiérarchiser les informations, organiser les pages et rendre le parcours de contact compréhensible.</dd></div>
        <div><dt>Mobile et accessibilité</dt><dd>Adapter les mises en page aux écrans, vérifier les contrastes, la navigation au clavier et les libellés. Une charte ne dispense pas de ces contrôles.</dd></div>
        <div><dt>Intégration et livraison</dt><dd>Développer les pages, intégrer les contenus et tester les fonctions prévues. Le périmètre exact reste celui du devis du site.</dd></div>
      </dl>
      <p class="scope-question"><strong>À demander</strong> « Que faut-il adapter pour le web, qui le valide et quels tests sont prévus ? »</p>
    </div>
  </details>
  <p class="scope-footnote">Une charte graphique ne remplace pas la conception du site ; le devis du site ne comprend pas automatiquement la création d’une charte.</p>
</div>

<style>
  .identity-scope { color: var(--color-gray-600); background: var(--color-snow-50); border: 1px solid var(--color-gray-200); border-radius: 1rem; padding: clamp(1.25rem, 4vw, 2rem); }
  .identity-scope .scope-kicker { color: var(--color-blue-500); font-size: .75rem; font-weight: 500; text-transform: uppercase; letter-spacing: .08em; margin: 0 0 .5rem; }
  .identity-scope .scope-intro { color: var(--color-gray-900); font-weight: 500; font-size: 1.25rem; line-height: 1.35; letter-spacing: -.01em; margin: 0; }
  .identity-scope .scope-hint { font-size: .875rem; color: var(--color-gray-600); margin: .4rem 0 1.25rem; }
  .identity-scope details { border: 1px solid var(--color-gray-200); border-radius: .75rem; background: var(--color-snow-50); transition: border-color 160ms ease; }
  .identity-scope details + details { margin-top: .75rem; }
  .identity-scope details[open] { border-color: var(--color-blue-100); }
  .identity-scope summary { display: flex; align-items: center; gap: .85rem; padding: .85rem 1rem; min-height: 44px; list-style: none; cursor: pointer; color: var(--color-gray-900); font-weight: 500; line-height: 1.4; border-radius: .75rem; }
  .identity-scope summary::-webkit-details-marker { display: none; }
  .identity-scope summary::after { content: ""; flex: none; width: .5rem; height: .5rem; margin: 0 .25rem 0 auto; border-right: 1.5px solid var(--color-gray-500); border-bottom: 1.5px solid var(--color-gray-500); transform: translateY(-2px) rotate(45deg); transition: transform 200ms ease; }
  .identity-scope details[open] > summary::after { transform: translateY(2px) rotate(-135deg); }
  .identity-scope summary:hover { background: var(--color-surface-100); }
  .identity-scope summary:focus-visible { outline: 2px solid var(--color-blue-500); outline-offset: 2px; }
  .scope-number { flex: none; display: grid; place-items: center; width: 1.9rem; height: 1.9rem; border-radius: .5rem; background: var(--color-blue-50); color: var(--color-blue-600); font-size: .75rem; font-variant-numeric: tabular-nums; }
  .scope-content { padding: 0 1rem 1rem; }
  .scope-content dl { margin: 0; }
  .scope-content dl > div { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 2fr); gap: 1rem; padding-block: .85rem; border-top: 1px solid var(--color-gray-100); font-size: .9375rem; line-height: 1.6; }
  .scope-content dt { color: var(--color-gray-900); font-weight: 500; }
  .scope-content dd { margin: 0; }
  .identity-scope .scope-question { margin: .5rem 0 0; padding: .85rem 1rem; border-radius: .6rem; background: var(--color-blue-50); color: var(--color-gray-700); font-size: .9375rem; line-height: 1.6; }
  .identity-scope .scope-question strong { display: block; margin-bottom: .15rem; color: var(--color-blue-600); font-size: .75rem; font-weight: 500; letter-spacing: .06em; text-transform: uppercase; }
  .identity-scope .scope-footnote { font-size: .8125rem; line-height: 1.6; color: var(--color-gray-600); margin: 1rem 0 0; }
  @media (max-width: 480px) { .scope-content dl > div { grid-template-columns: minmax(0, 1fr); gap: .25rem; } }
  @media (prefers-reduced-motion: reduce) { .identity-scope details, .identity-scope summary::after { transition: none; } }
</style>
