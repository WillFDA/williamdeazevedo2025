<aside class="persistance-partner" aria-label="Identité visuelle et site : deux métiers complémentaires">
  <figure>
    <img
      src="/articles/exemples-sites-vitrines/persistance-home.webp"
      alt="Capture de l’accueil Persistance : fond clair, grand titre noir et photographie d’un travail graphique sur papier."
      width="1920"
      height="1080"
      loading="lazy"
      decoding="async"
    />
    <figcaption>Capture issue du portfolio : partie supérieure de l’accueil de Persistance, avec ses accès aux offres et aux contenus. Il s’agit du site réalisé, pas d’une maquette de charte graphique.</figcaption>
  </figure>
  <div class="partner-note">
    <p class="partner-eyebrow">Du langage visuel au site</p>
    <p class="partner-title">Une identité à reconnaître.<br />Un site à parcourir.</p>
    <dl class="partner-roles">
      <div>
        <dt><span aria-hidden="true">01</span> Persistance · identité visuelle</dt>
        <dd>Logo, palette, typographies et charte : des repères cohérents pour vos supports de communication.</dd>
      </div>
      <div>
        <dt><span aria-hidden="true">02</span> William · conception du site</dt>
        <dd>Je pars de cette base pour organiser vos contenus, concevoir les pages et intégrer le site, avec ses adaptations mobiles et ses vérifications d’accessibilité.</dd>
      </div>
    </dl>
    <p>Si vous avez besoin de créer ou de faire évoluer votre identité, je recommande Persistance, le studio de Cédrine Marmasse avec lequel je collabore. La charte graphique fait l’objet d’un devis distinct du budget du site. Vous pouvez aussi venir avec votre identité existante ou travailler avec un autre designer.</p>
    <a class="partner-link" href="https://persistance-studio.fr/identite-visuelle/">Découvrir la prestation d’identité visuelle de Persistance <span aria-hidden="true">↗</span></a>
  </div>
</aside>

<style>
  .persistance-partner {
    border: 1px solid var(--color-gray-200);
    border-radius: 1rem;
    background: var(--color-snow-500);
    overflow: hidden;
    color: var(--color-gray-700);
  }
  .persistance-partner figure { margin: 0; }
  .persistance-partner figure img {
    display: block;
    width: 100%;
    height: auto;
    margin: 0;
    border-radius: 0;
  }
  .persistance-partner figcaption {
    padding: .85rem 1.25rem;
    font-size: .8rem;
    line-height: 1.6;
    border-bottom: 1px solid var(--color-gray-200);
  }
  .partner-note { padding: clamp(1.25rem, 4vw, 2rem); }
  .persistance-partner .partner-eyebrow {
    color: var(--color-blue-700);
    font-size: .75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: .09em;
    margin: 0 0 .65rem;
  }
  .persistance-partner .partner-title {
    color: var(--color-gray-900);
    font-size: clamp(1.35rem, 4vw, 1.8rem);
    line-height: 1.25;
    letter-spacing: -.025em;
    font-weight: 500;
    margin: 0;
  }
  .partner-roles { margin: 1.5rem 0; }
  .partner-roles > div { padding: 1rem 0; border-top: 1px solid var(--color-gray-200); }
  .partner-roles dt { color: var(--color-gray-900); font-weight: 600; }
  .partner-roles dt span { color: var(--color-blue-700); margin-right: .5rem; font-variant-numeric: tabular-nums; }
  .partner-roles dd { margin: .4rem 0 0; }
  .persistance-partner .partner-link {
    display: inline-flex;
    align-items: center;
    gap: .65rem;
    min-height: 44px;
    margin-top: .75rem;
    padding-block: .45rem;
    color: var(--color-blue-700);
    text-decoration-color: currentColor;
    text-underline-offset: .2em;
  }
  .partner-link span { transition: transform 160ms ease; }
  .partner-link:hover span { transform: translate(2px, -2px); }
  .partner-link:focus-visible { outline: 2px solid var(--color-blue-500); outline-offset: 4px; border-radius: .2rem; }
  @media (prefers-reduced-motion: reduce) {
    .partner-link span { transition: none; }
    .partner-link:hover span { transform: none; }
  }
</style>
