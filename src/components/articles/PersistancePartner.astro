<aside class="persistance-partner" aria-label="Identité visuelle et site : deux métiers complémentaires">
  <figure>
    <img
      src="/articles/exemples-sites-vitrines/persistance-home.webp"
      alt="Capture de l’accueil Persistance : fond clair, grand titre noir et photographie d’un travail graphique sur papier."
      width="1920"
      height="1080"
      loading="lazy"
      decoding="async"
    />
    <figcaption>Capture issue du portfolio : partie supérieure de l’accueil de Persistance, avec ses accès aux offres et aux contenus. Il s’agit du site réalisé, pas d’une maquette de charte graphique.</figcaption>
  </figure>
  <div class="partner-note">
    <p class="partner-eyebrow">Du langage visuel au site</p>
    <p class="partner-title">Une identité à reconnaître.<br />Un site à parcourir.</p>
    <dl class="partner-roles">
      <div>
        <dt><span aria-hidden="true">01</span> Persistance · identité visuelle</dt>
        <dd>Logo, palette, typographies et charte : des repères cohérents pour vos supports de communication.</dd>
      </div>
      <div>
        <dt><span aria-hidden="true">02</span> William · conception du site</dt>
        <dd>Je pars de cette base pour organiser vos contenus, concevoir les pages et intégrer le site, avec ses adaptations mobiles et ses vérifications d’accessibilité.</dd>
      </div>
    </dl>
    <p>Si vous avez besoin de créer ou de faire évoluer votre identité, je recommande Persistance, le studio de Cédrine Marmasse avec lequel je collabore. La charte graphique fait l’objet d’un devis distinct du budget du site. Vous pouvez aussi venir avec votre identité existante ou travailler avec un autre designer.</p>
    <a class="partner-link" href="https://persistance-studio.fr/identite-visuelle/">Découvrir la prestation d’identité visuelle de Persistance <span aria-hidden="true">↗</span></a>
  </div>
</aside>

<style>
  .persistance-partner { border: 1px solid var(--color-gray-200); border-radius: 1rem; background: var(--color-snow-50); overflow: hidden; color: var(--color-gray-600); }
  .persistance-partner figure { margin: 0; }
  .persistance-partner figure img { display: block; width: 100%; height: auto; margin: 0; border-radius: 0; }
  .persistance-partner figcaption { padding: .75rem clamp(1.25rem, 4vw, 2rem); font-size: .8125rem; line-height: 1.6; color: var(--color-gray-600); border-block: 1px solid var(--color-gray-100); background: var(--color-surface-100); }
  .partner-note { padding: clamp(1.25rem, 4vw, 2rem); }
  .persistance-partner .partner-eyebrow { color: var(--color-blue-500); font-size: .75rem; font-weight: 500; text-transform: uppercase; letter-spacing: .08em; margin: 0 0 .5rem; }
  .persistance-partner .partner-title { color: var(--color-gray-900); font-size: clamp(1.25rem, 3.5vw, 1.5rem); line-height: 1.3; letter-spacing: -.02em; font-weight: 500; margin: 0; }
  .persistance-partner .partner-roles { display: grid; gap: .75rem; margin: 1.5rem 0; }
  .partner-roles > div { padding: 1rem 1.1rem; border-radius: .75rem; background: var(--color-surface-100); }
  .partner-roles dt { display: flex; align-items: center; gap: .65rem; color: var(--color-gray-900); font-weight: 500; line-height: 1.4; }
  .partner-roles dt span { flex: none; display: grid; place-items: center; width: 1.75rem; height: 1.75rem; border-radius: .5rem; background: var(--color-blue-50); color: var(--color-blue-600); font-size: .75rem; font-variant-numeric: tabular-nums; }
  .partner-roles dd { margin: .6rem 0 0; font-size: .9375rem; line-height: 1.6; }
  .partner-note > p:not([class]) { font-size: .9375rem; line-height: 1.7; margin: 0; }
  .persistance-partner .partner-link { display: inline-flex; align-items: center; gap: .6rem; min-height: 44px; margin-top: 1.25rem; padding: .55rem 1rem; border: 1px solid var(--color-gray-200); border-radius: .6rem; background: var(--color-snow-50); color: var(--color-gray-900); font-size: .875rem; font-weight: 500; line-height: 1.4; text-decoration: none; transition: background-color 160ms ease, border-color 160ms ease; }
  .persistance-partner .partner-link:hover { background: var(--color-surface-100); border-color: var(--color-gray-300); color: var(--color-gray-900); }
  .partner-link span { color: var(--color-blue-500); transition: transform 160ms ease; }
  .partner-link:hover span { transform: translate(2px, -2px); }
  .partner-link:focus-visible { outline: 2px solid var(--color-blue-500); outline-offset: 3px; }
  @media (min-width: 640px) { .persistance-partner .partner-roles { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  @media (prefers-reduced-motion: reduce) {
    .partner-link, .partner-link span { transition: none; }
    .partner-link:hover span { transform: none; }
  }
</style>
