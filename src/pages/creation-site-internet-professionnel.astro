---
import SeoServicePage from "@/components/SeoServicePage.astro";
import { getServiceBySlug } from "@/data/services";

export const prerender = true;

const service = getServiceBySlug("creation-site-internet-professionnel");

if (!service) {
    throw new Error("Service not found: creation-site-internet-professionnel");
}
---

<SeoServicePage
    service={service}
    canonicalPath="/creation-site-internet-professionnel"
/>
